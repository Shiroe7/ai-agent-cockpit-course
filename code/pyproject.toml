[project]
name = "agent-cockpit-labs"
version = "0.1.0"
description = "Basic labs for LLM agents and cockpit applications"
requires-python = ">=3.10"
dependencies = []

[tool.unittest]
start-directory = "tests"
